<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.AmiFlix" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/amiflix_primary</item>
        <item name="colorPrimaryDark">@color/amiflix_primary_variant</item>
        <item name="colorAccent">@color/amiflix_secondary</item>
        
        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/black</item>
        
        <!-- Background colors -->
        <item name="android:windowBackground">@color/amiflix_background</item>
        <item name="android:colorBackground">@color/amiflix_background</item>
        
        <!-- Enable window content transitions -->
        <item name="android:windowContentTransitions">true</item>
        
        <!-- TV specific -->
        <item name="android:windowOverscan">true</item>
    </style>
    
    <!-- Player theme -->
    <style name="Theme.AmiFlix.Player" parent="Theme.AmiFlix">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/black</item>
    </style>
</resources> 