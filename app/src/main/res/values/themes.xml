<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.AmiFlix" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/amiflix_primary</item>
        <item name="colorPrimaryVariant">@color/amiflix_primary_variant</item>
        <item name="colorOnPrimary">@color/white</item>
        
        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/amiflix_secondary</item>
        <item name="colorSecondaryVariant">@color/amiflix_secondary_variant</item>
        <item name="colorOnSecondary">@color/black</item>
        
        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/black</item>
        
        <!-- Background colors -->
        <item name="android:windowBackground">@color/amiflix_background</item>
        <item name="colorSurface">@color/amiflix_surface</item>
        <item name="colorOnSurface">@color/white</item>
        
        <!-- Error colors -->
        <item name="colorError">@color/amiflix_error</item>
        <item name="colorOnError">@color/white</item>
        
        <!-- Enable window content transitions -->
        <item name="android:windowContentTransitions">true</item>
        
        <!-- TV specific -->
        <item name="android:windowOverscan">true</item>
    </style>
    
    <!-- Player theme -->
    <style name="Theme.AmiFlix.Player" parent="Theme.AmiFlix">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/black</item>
    </style>
</resources> 